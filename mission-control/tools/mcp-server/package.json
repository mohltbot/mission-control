{
  "name": "@openclaw/mcp-server",
  "version": "0.1.0",
  "description": "MCP (Model Context Protocol) server for OpenClaw Mission Control — exposes missions, tasks, agents, and tools as MCP resources and tools",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "keywords": ["mcp", "openclaw", "mission-control", "agents"]
}
